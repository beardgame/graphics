using Bearded.Graphics.Textures;

namespace Bearded.Graphics.Pipelines
{
    public class PipelineDepthTexture : PipelineTextureBase
    {
        public PipelineDepthTexture(Texture texture) : base(texture)
        {
        }
    }
}